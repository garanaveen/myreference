----------------------------------------

@howto do rot13 encoding? #rot13, #ROT13
Select the block you want to encode. Type "g?" to do the encoding.
To encode the entire file do, "ggVGg?"

----------------------------------------

@howto make the @cursor in vim bright on a dark themed @terminal?
On @mac, Terminal->Settings->BlinkCursor and Change color to bright color.

----------------------------------------

