I use terminator to be able to split the terminal as per my needs.
tilda is xterm kind of terminal which doesn't have any kind of titlebar. Could be best used on an external monitor for say logging.
sudo apt-get install tilda

sudo apt-get install tilix
Change the theme to dark.

